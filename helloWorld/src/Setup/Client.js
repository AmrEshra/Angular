var Client = /** @class */ (function () {
    function Client(firstName, lastName) {
        this.firstName = firstName;
        this.lastName = lastName;
    }
    return Client;
}());
var myClient = new Client("Amr", "Ali");
console.log(myClient);
